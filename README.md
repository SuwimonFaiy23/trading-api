# Trading API

Trading API คือบริการที่พัฒนาโดยใช้ภาษา **Go (Golang)** สำหรับจัดการระบบซื้อขาย / คำสั่งซื้อ / ประมวลผลต่าง ๆ ภายในระบบ โดยโครงสร้างโปรเจ็กต์ถูกออกแบบให้รองรับการขยายตัวในอนาคต เช่น การทำงานแบบ Clean Architecture, แยกเลเยอร์ service / repository และรองรับการเชื่อมต่อฐานข้อมูลผ่าน GORM

---

## 🚀 Features
- REST API สำหรับจัดการข้อมูลภายในระบบซื้อขาย
- ออกแบบแยกเลเยอร์: **handler**, **service**, **repository**
- รองรับการเชื่อมต่อฐานข้อมูลผ่าน **GORM**
- รองรับการจัดการ Environment ด้วย `config.yaml` ร่วมกับ Viper
- เขียนด้วย Go version **1.23.4**
- ใช้งานร่วมกับ Docker ได้

## ⚙️ Installation

### 1. Clone Repo
```bash
git clone https://github.com/<username>/trading-api.git
cd trading-api

2. Install Dependencies
   go mod download

3. Run Application
   go run ./cmd/server

## 🛠 Configuration

ตั้งค่าฐานข้อมูลใน `config.yaml`:

```yaml
database:
  host: localhost
  port: 5432
  user: myuser
  pass: password
  name: mydb

Running PostgreSQL with Docker
docker run --name mydb \
  -e POSTGRES_PASSWORD=password \
  -e POSTGRES_DB=trading_db \
  -p 5432:5432 \
  -d postgres:16
